<nav>
    <ul class="pager">
        {% if pagination.prev %}
            <li><a href="{{pagination.prev.link}}">Previous</a></li>
        {% endif %}
        {% if pagination.next %}
            <li><a href="{{pagination.next.link}}">Next</a></li>
        {% endif %}
    </ul>
</nav>
